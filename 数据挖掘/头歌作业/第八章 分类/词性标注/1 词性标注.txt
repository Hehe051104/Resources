import jieba.posseg as psg
text=input()
#任务：使用jieba模块的函数对text完成词性标注并将结果存储到result变量中
# ********** Begin *********#
result = ' '.join([f'{w.word}/{w.flag}' for w in psg.cut(text)])

# ********** End **********#
print(result)